<div class="content-header">
    <p>Stores</p>
    <a href="dash#!/stores/add" class="btn pull-right">Add Store</a>
</div>

<div ng-controller="sideCtrl">
</div>
{{message}}

<div ng-repeat="store in stores">
    <a href="dash#!/{{store._id}}/categories/" ng-click="localStore()">
        <button class="btn-home">{{store.name}}</button>
    </a>
</div>